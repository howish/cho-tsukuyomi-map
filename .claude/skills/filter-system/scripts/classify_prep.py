#!/usr/bin/env python3
"""filter-system classify-prep — emit per-booth signal bundle JSON for
parallel-dispatched classification agents.

Stub: full implementation lands in Pass 4 of formalize-filter-system.
Pass 1 only includes validate.
"""

from __future__ import annotations

import sys


def main():
    print("classify-prep: not yet implemented (Pass 4 of formalize-filter-system).",
          file=sys.stderr)
    print("See openspec/changes/formalize-filter-system/tasks.md (group 6).",
          file=sys.stderr)
    sys.exit(2)


if __name__ == "__main__":
    main()
